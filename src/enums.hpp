
enum class AgoranStatus {
    StandBy,
    Working,
    Error,
    Disabled
};

enum class AgoranMode {
    Automatic,
    Manual
};

enum class side {
    left,
    right,
    center
};

enum class ActionType {
    Goto,
    Push,
    Take
};
