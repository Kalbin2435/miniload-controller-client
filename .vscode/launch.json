{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/build/project.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build-project"
        }
    ]
}